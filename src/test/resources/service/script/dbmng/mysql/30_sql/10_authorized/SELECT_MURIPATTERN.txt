SELECT
	MURIPATTERN_ID,
	URI_PATTERN_NAME,
	URI_PATTERN,
	VERSION,
	DEL_FLG,
	CREATE_USER,
	CREATE_DATE,
	UPDATE_USER,
	UPDATE_DATE
 FROM MURIPATTERN
 ORDER BY
	MURIPATTERN_ID,
	URI_PATTERN_NAME,
	URI_PATTERN,
	VERSION,
	DEL_FLG,
	CREATE_USER,
	CREATE_DATE,
	UPDATE_USER,
	UPDATE_DATE
;
