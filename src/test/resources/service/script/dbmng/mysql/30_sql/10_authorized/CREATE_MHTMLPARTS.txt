CREATE TABLE IF NOT EXISTS MHTMLPARTS(
	MHTMLPARTS_ID INT NOT NULL AUTO_INCREMENT,
	PARTS_NAME VARCHAR(256),
	VERSION INT,
	IS_DELETED INT,
	CREATED_BY VARCHAR(128),
	CREATED_AT DATETIME,
	UPDATED_BY VARCHAR(128),
	UPDATED_AT TIMESTAMP,
	PRIMARY KEY(MHTMLPARTS_ID)
);
