CREATE TABLE IF NOT EXISTS TROLEINACCOUNT(
	TROLEINACCOUNT_ID INT NOT NULL AUTO_INCREMENT,
	TACCOUNT_ID INT,
	MROLE_ID INT,
	VERSION INT,
	DEL_FLG INT,
	CREATE_USER VARCHAR(128),
	CREATE_DATE DATETIME,
	UPDATE_USER VARCHAR(128),
	UPDATE_DATE TIMESTAMP,
	PRIMARY KEY(TROLEINACCOUNT_ID)
);
