CREATE TABLE IF NOT EXISTS THTMLPARTSINROLE(
	THTMLPARTSINROLE_ID INT NOT NULL AUTO_INCREMENT,
	MROLE_ID INT,
	MHTMLPARTS_ID INT,
	AUTH_KIND VARCHAR(256),
	VERSION INT,
	DEL_FLG INT,
	CREATE_USER VARCHAR(128),
	CREATE_DATE DATETIME,
	UPDATE_USER VARCHAR(128),
	UPDATE_DATE TIMESTAMP,
	PRIMARY KEY(THTMLPARTSINROLE_ID)
);
