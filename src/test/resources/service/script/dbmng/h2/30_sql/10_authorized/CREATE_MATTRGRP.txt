CREATE TABLE IF NOT EXISTS MATTRGRP(
	MATTRGRP_ID INT NOT NULL AUTO_INCREMENT,
	ATTRGRP_NAME VARCHAR(256),
	VERSION INT,
	DEL_FLG INT,
	CREATE_USER VARCHAR(128),
	CREATE_DATE DATETIME,
	UPDATE_USER VARCHAR(128),
	UPDATE_DATE TIMESTAMP,
	PRIMARY KEY(MATTRGRP_ID)
);
