CREATE TABLE IF NOT EXISTS TATTRINSUBSTANCE(
	TATTRINSUBSTANCE_ID INT NOT NULL AUTO_INCREMENT,
	TSUBSTANCE_ID INT,
	MATTR_ID INT,
	VERSION INT,
	IS_DELETED INT,
	CREATED_BY VARCHAR(128),
	CREATED_AT DATETIME,
	UPDATED_BY VARCHAR(128),
	UPDATED_AT TIMESTAMP,
	PRIMARY KEY(TATTRINSUBSTANCE_ID)
);
